import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Contacts",
};

const Contacts = () => {
  return (
    <div>
      <h1>Contacts</h1>
    </div>
  );
};

export default Contacts;
